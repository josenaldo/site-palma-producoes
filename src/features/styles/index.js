export { default as theme } from './theme'

export {
  default as palette,
  primaryColor,
  secondaryColor,
  surficeColor,
} from './palette'

export { default as ThemeProvider } from './ThemeProvider'
